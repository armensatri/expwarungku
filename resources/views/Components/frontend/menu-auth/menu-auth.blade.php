<a href="{{ $route }}" class="inline-flex ml-4">
  <div class="flex items-center w-full space-x-3 text-gray-700 hover:text-blue-600">

    <img src="{{ $image }}"
      alt="{{ $alt }}"
      class="size-4"
    />

    <span>{{ $menu }}</span>
  </div>
</a>
